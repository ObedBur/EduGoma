{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Redéfinition de outDir pour le build, même si déjà dans tsconfig.json
    "outDir": "./dist" 
  },
  "exclude": [
    "node_modules",
    "test",
    "dist",
    "**/*spec.ts"
  ]
}