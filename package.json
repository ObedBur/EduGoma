{
  "name": "edugoma",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors blue,green \"bun run --filter server dev\" \"bun run --filter client dev\"",
    "dev:server": "bun run --filter server dev",
    "dev:client": "bun run --filter client dev",
    "build": "bun run --filter server build && bun run --filter client build",
    "lint": "bun run --filter server lint && bun run --filter client lint"
  },
  "devDependencies": {
    "@swc/cli": "^0.7.10",
    "@swc/core": "^1.16.2",
    "concurrently": "^9.1.0"
  }
}
